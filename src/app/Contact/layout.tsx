"use client";

import ContactForm from "@/app/components/ContactForm";import React from "react";

const Layout = ({children }: { children: React.ReactNode }) => {
  return (
    <>
    <ContactForm/>
      </>
  );
};

export default Layout; 